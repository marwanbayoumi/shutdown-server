{
  "name": "shutdow-script",
  "version": "1.0.0",
  "description": "a simple express server that sends a shutdown sig among other things",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "js",
    "server",
    "cmd"
  ],
  "author": "marwanb.",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  }
}
